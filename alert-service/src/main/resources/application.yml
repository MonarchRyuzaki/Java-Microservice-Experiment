server:
  port: 8082
spring:
  application:
    name: alert-service
  datasource:
    url: jdbc:postgresql://localhost:5432/coin_db
    username: admin
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
grpc:
  client:
    market-data-client:
      address: static://localhost:9090
      negotiation-type: plaintext
