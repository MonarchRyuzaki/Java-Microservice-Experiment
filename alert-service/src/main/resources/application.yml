server:
  port: 8082
spring:
  application:
    name: alert-service
  datasource:
    url: jdbc:postgresql://localhost:5432/coin_db
    username: admin
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true